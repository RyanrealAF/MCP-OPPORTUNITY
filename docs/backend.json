
{
  "entities": {
    "MCP": {
      "title": "MCP",
      "description": "A Managed Capability Provider representing an external integration or internal service.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "description": { "type": "string" },
        "iconUrl": { "type": "string", "description": "AI-generated industrial icon for the provider." },
        "explicitCapabilities": {
          "type": "array",
          "items": { "type": "string" }
        },
        "implicitCapabilities": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "name": { "type": "string" },
              "description": { "type": "string" }
            }
          }
        },
        "version": { "type": "string" },
        "status": { "type": "string", "enum": ["active", "deprecated", "experimental"] },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["name", "description", "status"]
    },
    "Goal": {
      "title": "Goal",
      "description": "A strategic objective that requires one or more MCP capabilities.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "title": { "type": "string" },
        "status": { "type": "string", "enum": ["pending", "analyzing", "complete"] },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["title", "status"]
    },
    "Simulation": {
      "title": "Simulation",
      "description": "Results from a combinatorial collision analysis.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "timestamp": { "type": "string", "format": "date-time" },
        "results": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "name": { "type": "string" },
              "description": { "type": "string" },
              "noveltyRank": { "type": "number" }
            }
          }
        }
      },
      "required": ["timestamp", "results"]
    },
    "EvolutionHistory": {
      "title": "Evolution History",
      "description": "A record of code modifications applied via the Evolution Agent.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "target": { "type": "string" },
        "description": { "type": "string" },
        "timestamp": { "type": "string", "format": "date-time" },
        "status": { "type": "string", "enum": ["proposed", "applied", "rejected"] }
      },
      "required": ["target", "description", "timestamp", "status"]
    }
  },
  "auth": {
    "providers": ["google.com"]
  },
  "firestore": {
    "/users/{userId}/mcps/{mcpId}": {
      "schema": { "$ref": "#/backend/entities/MCP" },
      "description": "User-specific MCP registry."
    },
    "/users/{userId}/goals/{goalId}": {
      "schema": { "$ref": "#/backend/entities/Goal" },
      "description": "User-specific strategic goals."
    },
    "/users/{userId}/simulations/{simId}": {
      "schema": { "$ref": "#/backend/entities/Simulation" },
      "description": "Historical collision agent simulation results."
    },
    "/users/{userId}/evolution_logs/{logId}": {
      "schema": { "$ref": "#/backend/entities/EvolutionHistory" },
      "description": "Logs of ecosystem modifications."
    }
  }
}
